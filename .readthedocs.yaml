version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    post_install:
      - uv pip install ".[docs]"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - method: uv
      command: sync
